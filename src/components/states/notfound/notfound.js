import React from 'react';
import Box from '@material-ui/core/Box'

const notFound = () => {
    return (        <Box display="flex" justifyContent="center">
    <p>
        can't find github acccount with the username


    </p>
    </Box>);
}

export default notFound